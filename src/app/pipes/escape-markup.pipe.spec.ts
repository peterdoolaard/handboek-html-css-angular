import { EscapeMarkupPipe } from './escape-markup.pipe';

describe('EscapeMarkupPipe', () => {
  it('create an instance', () => {
    const pipe = new EscapeMarkupPipe();
    expect(pipe).toBeTruthy();
  });
});
